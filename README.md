# Design Theme Library

A design system admin tool for managing themes, tokens, and components.

## Setup

```bash
npm install
npm run dev
```

## Environment

Copy `.env.local.example` to `.env.local` and add your Supabase credentials.

